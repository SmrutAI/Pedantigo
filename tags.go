package pedantigo

import "reflect"

// parseTag parses a struct tag and returns constraints
func parseTag(tag reflect.StructTag) map[string]string {
	// TODO: implement tag parsing
	// Parse validate:"required,email,min=18" into map
	return nil
}
